type Query {
    authors: [Author]
    books: [Book]
    bookById(id: ID): Book
    authorById(id: ID): Author
}

type Book {
    id: ID
    title: String
    pageCount: Int
    author: Author
}

type Author {
    id: ID
    firstName: String
    lastName: String
}
